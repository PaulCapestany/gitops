apiVersion: v1
kind: Service
metadata:
  name: {{ include "toy-service.fullname" . }}
  labels:
    app: {{ include "toy-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
  selector:
    app: {{ include "toy-service.name" . }}